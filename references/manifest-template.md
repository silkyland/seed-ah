# Seed Manifest Template

Write to `SEED_MANIFEST.md` (repo root or `docs/`). This file is the
answer to "what's in this demo database and how do I log in?" — the
summary the user asked for, in one place, current after every seed run.

---

```markdown
# Seed Manifest

> ⚠️ Contains demo credentials by design. The accounts exist only in the
> demo database and use the reserved `@demo.example` domain — but keep
> this file out of public repos unless that database is disposable.
> Generated by seed-ah on <date>. Random seed: <value> (re-runs reproduce
> this exact world).

## Target

- Database: `<name>` on `<host>` (<env> — production gate confirmed
  <date>, live identity re-checked at run time)
- Marker for seeded rows: `<email domain / slug prefix / tag column>`
  (matched 0 pre-existing rows before seeding)
- Wipe round-trip: proven <date> — one-row skeleton seeded, wiped, all
  counts returned to baseline before the mass seed

## Demo accounts

| Login (email) | Password | Role | Good for demoing |
|---------------|----------|------|------------------|
| admin@demo.example | <password> | admin | settings, user management |
| somchai.editor@demo.example | <password> | editor | the full publish flow (rich history: 24 articles) |
| malee.member@demo.example | <password> | member | reader experience, comments |
| newuser@demo.example | <password> | member | EMPTY account — onboarding & empty states |

All passwords hashed via <the app's hasher> (`file:line`). Every account
above was login-tested on <date>.

## What was seeded

| Table | Rows | Shape notes |
|-------|-----:|-------------|
| users | 12 | 4 roles; 1 empty; Thai locale names |
| articles | 80 | power-law by author; statuses across enum; timestamps over 12 months |
| comments | 340 | heavier on popular articles; includes unicode/emoji edge rows |
| media | 96 | covers 1200×630 + avatars 256×256, generated (see below) |

## Images

- <N> covers: AI-generated, domain-themed / programmatic brand-color
  renders (source noted per batch) → `<storage path>`
- <N> avatars: deterministic initials-avatars
- Seed assets folder: `<path>` (self-contained; wipe removes copies)

## Edge-case rows (for demo drivers)

- `<entity>` "<title>" — longest-title boundary case
- `<entity>` — 0-items boundary
- …

## Commands

- **Re-seed (idempotent):** `<command>`
- **Wipe demo data:** `<command>` — deletes by marker, children first,
  prints counts
- **Verify:** `<command or steps>` — row counts, FK orphan check, demo
  logins

## Drift & findings

<Anything the census surfaced: migration/DB drift, validation stricter
than schema, lookup tables owned elsewhere, insert-time side effects
muted or accepted (observers, triggers, queued jobs) — with file:line.>
```

---

## Reporting rule

After every run, ALSO print the two tables that matter (demo accounts +
seeded counts) directly in the chat response — the user should never have
to open the file to learn the admin password.
